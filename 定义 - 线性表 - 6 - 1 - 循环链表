#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>

//循环链表
//是一种头尾相连的链表（即：表中最后一个结点的指针域指向头结点，整个链表形成一个环）
//优点：从表中任意结点出发均可找到表中其他结点
//空循环表：头结点的指针域指向头指针（自己指向自己）

//注意：由于循环链表中没有NULL指针，故涉及遍历操作时，其终止条件就不再像非循环链表那样判断 p 或 p->next 是否为空
//      而是判断他们是否等于头指针（p != L）
//头指针表示单循环链表：找 an 的时间复杂度为 O(n)，找 a1 时间复杂度为 O(1)   ---   不方便

//注意：表的操作常常是在表的首尾位置上进行
//尾指针表示单循环链表：a1的存储位置:R->next->next，an 的存储位置是：R   ---   时间复杂度都为 O(1)



int main()
{
	
	return 0;
}
